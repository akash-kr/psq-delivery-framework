# Optional. This webhook's configured Slack destination receives the default
# `delivery` channel. Add named channel entries and environment variables in
# watcher.rules.yml when different rules must route to different destinations.
# When unset, alerts fall back to stdout for cron/CI observability.
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/replace/me
